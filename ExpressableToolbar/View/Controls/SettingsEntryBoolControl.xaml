<UserControl x:Class="ExpressableToolbar.View.Controls.SettingsEntryBoolControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ExpressableToolbar.View.Controls"
             xmlns:vm="clr-namespace:ExpressableToolbar.ViewModel"
             xmlns:properties="clr-namespace:ExpressableToolbar.Properties"
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
	<Border Margin="0, 0, 0, 2"
	        Background="White">
		<Grid Height="52">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*"/>
				<ColumnDefinition Width="3*"/>
				<ColumnDefinition Width="Auto"/>
			</Grid.ColumnDefinitions>
			<TextBlock VerticalAlignment="Center" Margin="20, 0, 0, 0" Grid.Column="0" Text="{Binding Title}"/>
			<TextBlock VerticalAlignment="Center" Grid.Column="1" Text="{Binding Description}"/>
			<CheckBox VerticalAlignment="Center" Margin="0, 0, 20, 0" Grid.Column="2" Checked="{Binding Value}"/>
		</Grid>
	</Border>
</UserControl>
